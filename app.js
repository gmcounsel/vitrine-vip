const phone='5551981093326';let products=[];const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function render(filter='Todos'){const root=document.querySelector('#products');const list=products.filter(p=>filter==='Todos'||p.category===filter);root.innerHTML=list.map(p=>{const sold=p.status==='Vendido';const msg=encodeURIComponent(`Olá! Vi a Vitrine VIP e quero a ${p.code}. Ela ainda está disponível?`);return `<article class="card"><img src="${esc(p.image)}" alt="${esc(p.code)}"><div class="info"><div class="code">${esc(p.code)}</div><div class="material">${esc(p.material||'')}</div>${sold?'<div class="sold">VENDIDO</div>':`<div class="price">${esc(p.price||'')}</div><a class="buy" href="https://wa.me/${phone}?text=${msg}">QUERO ESTA PEÇA</a>`}</div></div></article>`}).join('')||'<p>Nenhuma peça disponível nesta categoria.</p>'}fetch('products.json').then(r=>r.json()).then(data=>{products=data;render()});document.querySelectorAll('[data-filter]').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.filters button').forEach(x=>x.classList.remove('active'));b.classList.add('active');render(b.dataset.filter)}));
