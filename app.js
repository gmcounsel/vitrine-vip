const phone='5551981093326';let products=[];const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function render(filter='Todos'){const root=document.querySelector('#products');const list=products.filter(p=>p.visible!==false&&(filter==='Todos'||p.category===filter));root.innerHTML=list.map(p=>{const sold=p.status==='Vendido';const msg=encodeURIComponent(`Olá! Vi a Vitrine VIP e quero a ${p.code}. Ela ainda está disponível?`);return `<article class="card"><img src="${esc(p.image)}" alt="${esc(p.code)}" loading="lazy"><div class="info"><div class="code">${esc(p.code)}</div>${p.category?`<div class="category">${esc(p.category)}</div>`:''}${p.material?`<div class="material">${esc(p.material)}</div>`:''}${p.price?`<div class="price">${esc(p.price)}</div>`:''}${sold?'<div class="sold">VENDIDO</div>':`<a class="buy" href="https://wa.me/${phone}?text=${msg}">QUERO ESTA PEÇA</a>`}</div></article>`}).join('')||'<p>Nenhuma peça disponível nesta categoria.</p>'}fetch('products.json').then(r=>{if(!r.ok)throw new Error('Não foi possível carregar o catálogo.');return r.json()}).then(data=>{products=data;render()}).catch(e=>{document.querySelector('#products').innerHTML='<p>Não foi possível carregar a Vitrine agora.</p>';console.error(e)});document.querySelectorAll('[data-filter]').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.filters button').forEach(x=>x.classList.remove('active'));b.classList.add('active');render(b.dataset.filter)}));
